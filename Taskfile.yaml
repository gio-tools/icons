# https://taskfile.dev

version: '3'

tasks:
  default:
    - task: fmt
    - go build -ldflags="-s -w"
    - task: lint

  nowayland:
    - task: fmt
    - go build -ldflags="-s -w" -tags nowayland
    - task: lint

  fmt:
    - goimports -w -l .
    - gofumpt -w -l .

  lint:
    - go vet ./...
